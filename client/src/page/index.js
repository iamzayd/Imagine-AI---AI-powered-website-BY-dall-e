// for exporting the files in pages folder
// so we can import in one line in out App.jsx

import Home from './Home';
import CreatePost from './CreatePost';
import Welcome from './Welcome';

export{
    Home,
    CreatePost,
    Welcome
}